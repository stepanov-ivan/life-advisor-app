## 1. Контракт LLM: переход на grams-only

- [x] 1.1 Обновить контракт `LLMClient.EstimateItemResult`: удалить `unit`, переименовать `quantity` в `grams` и сделать `grams` обязательным (`> 0`).
- [x] 1.2 Обновить `estimateMeal` system prompt под strict grams-only формат (`name` + `grams`) и убрать упоминания единиц `g/ml/pcs`.
- [x] 1.3 Обновить валидацию ответа LLM: валидировать чистоту `name`, обязательность `grams`, и сохранять fail-fast поведение (`parseFailed` при нарушении контракта).
- [x] 1.4 Обновить фикстуры и тесты контракта (`LLMContractTests`) под новый формат `grams`.

## 2. Модель и хранилище: quantity -> grams, удаление unit

- [x] 2.1 В `EstimateItem` удалить `unit/unitRaw` и переименовать `quantity` в `grams`.
- [x] 2.2 Переименовать baseline-поле `baseQuantity` в `baseGrams` и согласовать инициализацию baseline-значений.
- [x] 2.3 Обновить все места создания/заполнения `EstimateItem` (runtime, memory-suggestion path, editor apply) под новые поля.
- [x] 2.4 Подтвердить destructive schema change: запуск с пересозданием контейнера при несовместимой схеме без data migration.

## 3. UI редактора состава: убрать reset и перейти на grams-only

- [x] 3.1 Удалить кнопку `Сбросить к автопересчёту` из секции `Состав блюда`.
- [x] 3.2 Убрать колонку `ед` и переименовать колонку количества в `г`.
- [x] 3.3 Перевести `ItemDraft` и биндинги редактора на поле `grams`.
- [x] 3.4 Удалить отображение индикатора `Ручной режим` как лишний UX-шум.
- [x] 3.5 Обеспечить прием дробных значений `grams` и форматирование отображения `Ккал/Б/Ж/У` до 1 знака после запятой.

## 4. Логика автопересчёта и lock-поведение

- [x] 4.1 Обновить формулу автопересчёта на `scale = grams / baseGrams` (не от 100г).
- [x] 4.2 Зафиксировать single-path поведение: любое изменение `grams` снимает lock и перезаписывает ручные макросы автопересчётом.
- [x] 4.3 Сохранить full precision в модели, округление применять только на уровне UI-отображения.

## 5. Проверка и регрессии

- [x] 5.1 Обновить unit/integration тесты редактора и runtime под `grams/baseGrams` и удаление unit-полей.
- [x] 5.2 Добавить/обновить тест сценария перезаписи ручных макросов при изменении `grams`.
- [x] 5.3 Добавить/обновить тест сценария strict fail при невалидном контракте item (`name`/`grams`).
- [ ] 5.4 Выполнить post-reset smoke-check: чистый запуск приложения, создание приёма пищи, проверка сохранения `name + grams` и корректного автопересчёта.
