## 1. RuleEngine — форматирование значений и описания нарушений

- [x] 1.1 Создать структуру `RuleMetricDisplay` с полями `valueFormatted`, `thresholdFormatted`, `unit`, `zone`
- [x] 1.2 Реализовать маппинг `field → (unit, multiply, decimals)` в `RuleEngine`
- [x] 1.3 Реализовать метод `formattedMetric(for: RuleDefinition, date: Date) -> RuleMetricDisplay` — вычисляет значение правила, применяет формат
- [x] 1.4 Реализовать метод `violationDescription(for: RuleViolation) -> String` — генерирует русское описание по `reasonCode` и данным правила
- [x] 1.5 Для presence-правил: русские названия категорий (`whole_grain` → «цельные злаки», `legume_or_nut` → «бобовые/орехи/семена», `processed_meat` → «обработанное мясо»)
- [x] 1.6 Для countSkipped: форматирование количества пропусков в строку

## 2. DayAnalyticsPanel — числовые значения и сводная строка

- [x] 2.1 Переработать строку правила: справа показывать `valueFormatted + " из " + thresholdFormatted` (range) или зоновый текст (presence) или «X пропусков из ≤Y» (countSkipped)
- [x] 2.2 Число справа окрашивать в цвет зоны (через `zoneColor`)
- [x] 2.3 Добавить сводную строку «N из M правил соблюдаются» над группами
- [x] 2.4 Цвет сводной строки — по худшей зоне среди всех правил

## 3. MealSlotCard — русские описания и подсветка продуктов

- [x] 3.1 Заменить `violation.reasonCode` на `engine.violationDescription(for: violation)` в списке нарушений
- [x] 3.2 Формат строки нарушения: название правила + максимальный вклад приёма — «Свободный сахар — 45% дневного»
- [x] 3.3 Собрать словарь `[itemID: [(ruleId, ruleTitle, percent, zone)]]` из `contributionJSON` нарушений приёма
- [x] 3.4 Для каждого продукта с нарушениями: добавить цветной кружок и обернуть в `DisclosureGroup`
- [x] 3.5 Внутри `DisclosureGroup`: список строк «Правило — X% дневного» с цветовым индикатором
- [x] 3.6 Обработать случай без нарушений — «Все правила соблюдены» зелёным

## 4. RuleDetailView — русские описания

- [x] 4.1 В списке нарушений заменить `violation.reasonCode` на `engine.violationDescription(for: violation)`
- [x] 4.2 Убедиться, что строка «Причина: ...» больше не показывает английские коды

## 5. Проверка

- [x] 5.1 Собрать приложение, убедиться в отсутствии ошибок компиляции
- [ ] 5.2 Проверить на симуляторе: панель дашборда показывает числа
- [ ] 5.3 Проверить на симуляторе: карточка приёма показывает русские описания нарушений
- [ ] 5.4 Проверить на симуляторе: продукт-нарушитель раскрывается со списком нарушений
- [ ] 5.5 Проверить на симуляторе: сводная строка «N из M» корректна
