## 1. Навигация по датам на дашборде

- [ ] 1.1 Добавить состояние выбранной даты в `DashboardView` и хранение `lastSelectedDate` в `@AppStorage` (формат day-key `yyyy-MM-dd`).
- [ ] 1.2 Реализовать fallback на `today`, если восстановленная дата из `AppStorage` больше текущей даты.
- [ ] 1.3 Реализовать фильтрацию `MealEvent` только по выбранному дню (`startOfDay...endOfDay`) в локальной таймзоне.
- [ ] 1.4 Добавить week-strip на 7 дней (календарная неделя `Пн...Вс`) и выбор дня из него.
- [ ] 1.5 Добавить кнопку календаря и выбор произвольной даты с перестроением week-strip вокруг выбранной недели.
- [ ] 1.6 Запретить выбор будущих дат в week-strip и календаре.
- [ ] 1.7 Добавить пользовательский хинт «Будущие даты недоступны» при попытке выбора будущей даты.

## 2. Упрощение карточки и блока совета

- [ ] 2.1 Удалить с основной карточки приёма пищи отображение технических меток `Уверенность` и `Из памяти`.
- [ ] 2.2 Переименовать пользовательский блок `Почему этот совет` в `Совет`.
- [ ] 2.3 Убрать из UI совета технические поля (`источник структуры`, `уверенность модели`, `режим оценки`) и убрать кнопку `Показать всё`.
- [ ] 2.4 Добавить helper полезности совета (trim, минимальная длина >= 40, фильтр служебных шаблонов).
- [ ] 2.5 Реализовать правило: для memory-событий показывать `Совет` только если текст проходит критерий полезности.

## 3. Состав блюда и структура позиций

- [ ] 3.1 Переименовать секцию `Ручная корректировка по позициям` в `Состав блюда`.
- [ ] 3.2 Добавить заголовки таблицы `кол-во | ед | Ккал | Б | Ж | У` в редакторе события.
- [ ] 3.3 Расширить `EstimateItem` полями количества и единицы (`g/ml/pcs`) с дефолтом `g`.
- [ ] 3.4 Добавить поддержку дробных значений количества в UI и валидации модели.
- [ ] 3.5 Реализовать автопропорциональный пересчёт Ккал/Б/Ж/У при изменении количества (для незафиксированных вручную макросов).
- [ ] 3.6 Реализовать ручной override макросов с lock-поведением при последующем изменении количества.
- [ ] 3.7 Добавить `Сбросить к автопересчёту` для отдельной позиции.
- [ ] 3.8 Добавить `Сбросить к автопересчёту` для всего блюда.

## 4. Синхронизация текста и структуры

- [ ] 4.1 При изменении текста после существующей структуры помечать структуру как потенциально неактуальную дружелюбным текстом (без термина `out_of_sync`).
- [ ] 4.2 Сохранить правило: LLM-парсинг структуры выполняется только по кнопке `Пересчитать по тексту`.
- [ ] 4.3 Переименовать кнопку действия на `Пересчитать по тексту`.
- [ ] 4.4 Реализовать поведение ошибки LLM: сохранять текущую структуру без изменений и показывать inline-ошибку в редакторе.
- [ ] 4.5 Реализовать сценарий очистки всех позиций: подтверждение пользователя и перевод события в `pending-estimation` при сохранённом тексте.

## 5. Тестирование и регрессии

- [ ] 5.1 Добавить unit/integration тесты фильтрации событий по выбранной дате и восстановления `lastSelectedDate`.
- [ ] 5.2 Добавить unit тесты запрета будущих дат и fallback на `today`.
- [ ] 5.3 Добавить unit/integration тесты полезности совета и скрытия совета для memory-событий при неполезном тексте.
- [ ] 5.4 Добавить unit тесты модели `EstimateItem` для количества/единиц и lock-поведения ручных макросов.
- [ ] 5.5 Добавить UI-тест: выбор даты в календаре меняет отображаемые события выбранного дня.
- [ ] 5.6 Добавить UI-тест: будущая дата недоступна для выбора и показывает хинт.
- [ ] 5.7 Добавить интеграционный тест сценария `Пересчитать по тексту` при ошибке LLM (структура сохранена, inline-ошибка показана).
