## 1. Модель данных и миграция

- [x] 1.1 Добавить/обновить SwiftData-модели памяти: `MemorySuggestion`, `MemoryHypothesis`, `MemoryDataGap`.
- [x] 1.2 Расширить `MealEvent` полями `source` (`memory_suggestion`/`llm`/`manual_override`) и `out_of_sync`.
- [x] 1.3 Добавить версионность шаблонов memory-suggestion (текущая + fallback) с лимитом 3 версии.
- [x] 1.4 Реализовать destructive reset всего SwiftData store для новой схемы.
- [x] 1.5 Проверить старт приложения после reset с пустой памятью и без seed-инициализации.

## 2. Локальные подсказки и canonical-структуры

- [x] 2.1 Реализовать локальный индекс подсказок по последним 50 структурированным логам.
- [x] 2.2 Реализовать canonical-шаблон с alias-текстами для эквивалентных формулировок.
- [x] 2.3 Реализовать ранжирование подсказок по частоте и свежести.
- [x] 2.4 Ограничить выдачу максимум 5 подсказками.
- [x] 2.5 Реализовать подстановку текста + структуры по выбору подсказки без авто-отправки.
- [x] 2.6 Добавить сброс связанной структуры при очистке текста.

## 3. Поток логирования и синхронизация

- [x] 3.1 Реализовать правило «структура — источник истины, текст — ввод/заметка».
- [x] 3.2 Реализовать выставление `out_of_sync` при изменении текста после подстановки структуры.
- [x] 3.3 Реализовать сохранение с выбором при `out_of_sync`: `Переоценить по тексту` (default) / `Оставить структуру`.
- [x] 3.4 Реализовать поведение `Оставить структуру`: текст сохраняется как заметка без влияния на расчёт.
- [x] 3.5 Реализовать источник `manual_override` после ручной правки структуры.

## 4. Data-gap, confidence и уточнения

- [x] 4.1 Реализовать неблокирующее сохранение при отсутствии порции: `low confidence` + запись `data-gap`.
- [x] 4.2 Добавить UI быстрых уточнений порции (чипы) после выбора подсказки.
- [x] 4.3 Реализовать автоматическое закрытие `data-gap` после 2 повторных уточнений по похожему блюду.
- [x] 4.4 Реализовать retention для `data-gap`: удаление неактивных записей через 60 дней.

## 5. Гипотезы предпочтений и их lifecycle

- [x] 5.1 Реализовать генерацию гипотез после 2 повторяющихся сигналов.
- [x] 5.2 Добавить inline-подтверждение гипотез с действиями `Да / Нет / Спросить позже`.
- [x] 5.3 Реализовать `Спросить позже` с повторным показом не ранее 3 дней.
- [x] 5.4 Реализовать перевод подтверждённой гипотезы в мягкое предпочтение.
- [x] 5.5 Реализовать конфликтный режим: снижение веса предпочтения и статус `under_review`.
- [x] 5.6 Реализовать cooldown 7 дней для перепроверки подтверждённого предпочтения при конфликте.
- [x] 5.7 Реализовать retention для `pending_confirmation` гипотез: удаление через 60 дней неактивности.

## 6. Explainability и пользовательский контроль памяти

- [x] 6.1 Добавить бейдж `Из памяти` для структур, подставленных из memory-suggestion.
- [x] 6.2 Реализовать раскрываемый блок `Почему этот совет`.
- [x] 6.3 Показывать в explainability top-2 фактора с действием `Показать всё`.
- [x] 6.4 Добавить пояснение для `data-gap`: что не хватает и почему это важно для точности.
- [x] 6.5 Реализовать точечное удаление элементов памяти.
- [x] 6.6 Реализовать полный сброс памяти.

## 7. Обновление шаблонов после ручных правок

- [x] 7.1 Реализовать обновление основного шаблона только при изменении калорий <=30%.
- [x] 7.2 Реализовать сохранение правки >30% как `candidate` без мгновенного влияния.
- [x] 7.3 Реализовать promotion `candidate` в основной шаблон после 2+ повторов по canonical-группе.
- [x] 7.4 Реализовать сохранение предыдущего основного шаблона как fallback при promotion.

## 8. Тестирование и проверка регрессий

- [x] 8.1 Добавить тесты на source lifecycle (`memory_suggestion`/`llm`/`manual_override`) и `out_of_sync`.
- [x] 8.2 Добавить тесты на подсказки: окно 50, лимит 5, ранжирование частота+свежесть, alias/canonical.
- [x] 8.3 Добавить тесты на `data-gap` (создание, автозакрытие после 2 уточнений, retention 60 дней).
- [x] 8.4 Добавить тесты на гипотезы (`N=2`, `snooze=3d`, `under_review`, retention 60 дней).
- [x] 8.5 Добавить тесты на candidate/fallback-логику и лимит 3 версий шаблона.
- [x] 8.6 Добавить UI/интеграционные тесты на explainability-блок и выбор действий при `out_of_sync`.
