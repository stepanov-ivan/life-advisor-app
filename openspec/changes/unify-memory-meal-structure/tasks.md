## 1. Канонический pipeline и модель события

- [x] 1.1 Зафиксировать `MealEvent.items` как единый канон для состава блюда во всех путях сохранения (LLM и memory).
- [x] 1.2 Добавить/обновить metadata события: `structureOutdated`, `structureLastSyncedAt`, `baselineTextNormalized`, `source`.
- [x] 1.3 Обновить путь сохранения события: единый pipeline `text -> (валидные memory items ? use : LLM) -> canonical items -> save`.

## 2. Валидация памяти и fallback логика

- [x] 2.1 Реализовать фильтр валидных memory-записей для подсказок (`sourceText` валиден, normalized длина >= 3, валидные `items`).
- [x] 2.2 Исключать невалидные memory-записи (в т.ч. без исходной фразы) из подсказок без fallback и с debug-логом.
- [x] 2.3 Реализовать fallback в LLM при неполных/битых memory-items перед сохранением.
- [x] 2.4 Зафиксировать поведение ошибки fallback: не сохранять событие, возвращать ошибку пользователю.

## 3. Состояние синхронизации текста и структуры

- [x] 3.1 Реализовать нормализацию текста (trim + схлопывание пробелов) и сравнение с `baselineTextNormalized`.
- [x] 3.2 Устанавливать `structureOutdated=true` только при смысловом отличии normalized-текста.
- [x] 3.3 Реализовать явный пересчёт только по кнопке (без автозапуска от редактирования текста).
- [x] 3.4 Для no-op пересчёта (normalized текст не изменился) пропускать LLM-вызов и не обновлять `structureLastSyncedAt`.
- [x] 3.5 Успешный явный пересчёт: обновлять `items`, `baselineTextNormalized`, `structureOutdated=false`, `structureLastSyncedAt`.
- [x] 3.6 Ошибка явного пересчёта: оставлять текущие `items`, сохранять `structureOutdated=true`, разрешать сохранить событие без пересчёта.
- [x] 3.7 Ручная правка `items` не должна сбрасывать `structureOutdated`.

## 4. Подсветка калорийных позиций

- [x] 4.1 Реализовать расчет `kcalPer100g` на лету (`kcal / grams * 100`) без отдельного persisted-поля.
- [x] 4.2 Применить тройное правило подсветки для текущего события: `totalKcal >= 120`, `kcalPer100g >= 250`, `grams >= 10`.
- [x] 4.3 Применять подсветку ко всем позициям текущего события, не ограничивая top-N.

## 5. Reset и хранение версии схемы

- [x] 5.1 Реализовать one-shot auto-reset хранилища по версии схемы на старте приложения.
- [x] 5.2 Хранить флаг примененной версии reset в `UserDefaults`.
- [x] 5.3 Выполнить destructive reset без data migration и без пользовательского уведомления.

## 6. UI и поведение ввода

- [x] 6.1 При выборе memory-подсказки автозаполнять текст исходной фразой этой записи.
- [x] 6.2 Если валидных memory-подсказок нет, скрывать блок подсказок и оставлять только свободный ввод.
- [x] 6.3 Валидировать ввод: не сохранять событие при пустом normalized-тексте или длине < 3.

## 7. Тесты и smoke-проверка

- [x] 7.1 Обновить unit/integration тесты на единый pipeline сохранения и metadata синхронизации.
- [x] 7.2 Добавить тесты фильтра memory-записей (валидные/невалидные, включая отсутствие `sourceText`).
- [x] 7.3 Добавить тесты `Outdated` state-machine: пробелы/no-op/успех/ошибка явного пересчёта.
- [x] 7.4 Добавить тесты подсветки по тройному порогу (`totalKcal`, `kcalPer100g`, `grams`).
- [x] 7.5 Добавить тесты one-shot reset и хранения версии в `UserDefaults`.
- [ ] 7.6 Провести manual smoke-check: memory-path, fallback-path, сохранение при `Outdated`, поведение после reset.
