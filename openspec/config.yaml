schema: spec-driven

context: |
  Язык документации: русский (все артефакты — proposal, design, specs, tasks — писать на русском языке).
  Технологический стек: iOS (SwiftUI, SwiftData), локальное хранилище SQLite, интеграция с LLM API (DeepSeek/OpenAI).
  Домен: персональный трекинг жизни и поведенческая аналитика.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
